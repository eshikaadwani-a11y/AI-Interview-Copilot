# Lightweight dependency set for CI / running the test suite.
# Heavy/optional libraries (xgboost, shap, sklearn, chromadb,
# sentence-transformers, openai, anthropic) are imported lazily by the app and
# are NOT needed for the tests, which exercise the pure-Python + local paths.

fastapi==0.115.5
pydantic==2.9.2
pydantic-settings==2.6.1
python-multipart==0.0.17
email-validator==2.2.0
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0
motor==3.6.0
pymongo==4.9.2
httpx==0.27.2
python-dotenv==1.0.1
pytest==8.3.4
pytest-asyncio==0.24.0
